# 可复用组件模块

组件只负责显示与用户事件，不直接发请求、不直接访问路由参数。

- `WorkflowTimeline.vue`：按标准 `WorkflowRun` 契约显示步骤状态、进度、错误和结构化结果。
- `ModelEvaluationPanel.vue`：录入并展示某一模型配置的小样本成本、耗时、成功率和人工质量评分；HTTP 调用由父页面/Store 注入，组件不直接请求接口。

后续图片生成、视频生成等页面可复用同一个时间线组件；只要后端仍返回步骤状态机，组件无需知道模型供应商。

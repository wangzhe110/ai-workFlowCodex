# 状态模块

Pinia Store 负责页面间共享的项目数据、加载状态和错误状态。

- `project.ts`：项目各阶段数据、图片/视频生成、运行轮询与完成后的联动刷新。
- `creative-library.ts`：爆点元素和开头模式的查询、新增、软停用状态。
- `model-profiles.ts`：各步骤模型配置版本、供应商适配状态、无扣费预检、人工评测记录、同步骤对比与激活动作。

所有轮询由 Store 集中管理，页面卸载时必须调用 `stopPolling()`，避免用户离开页面后仍持续请求和消耗浏览器资源。

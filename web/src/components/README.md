# 可复用组件模块

组件只负责显示与用户事件，不直接发请求、不直接访问路由参数。

- `WorkflowTimeline.vue`：旧页面使用的标准 `WorkflowRun` 展示组件；V1 生产台自行逐阶段和逐镜头展示状态，避免把异步视频子任务压缩成单一时间线。
- `ModelEvaluationPanel.vue`：旧模型配置页面的小样本记录组件；V1 默认使用质量报表页面读取 `ModelInvocation` 和审核汇总。

组件只接受父页面传入的值和回调，不知道模型供应商、不保存 Key，也不直接请求接口。

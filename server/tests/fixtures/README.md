# 测试素材模块

本目录只保存由工程生成的无版权测试素材，不能放入用户视频、API Key 或任何真实生产内容。

`browser-upload-test.mp4` 是用于验证浏览器 `multipart/form-data` 上传链路的最小占位文件。当前 Day 1 的服务只校验扩展名和声明 MIME 类型；后续接入 `ffprobe` 后，应替换为真实但无版权的短视频夹具，并验证编码、时长、分辨率和文件大小限制。
